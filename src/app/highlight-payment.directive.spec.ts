import { HighlightPaymentDirective } from './highlight-payment.directive';

describe('HighlightPaymentDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPaymentDirective();
    expect(directive).toBeTruthy();
  });
});
